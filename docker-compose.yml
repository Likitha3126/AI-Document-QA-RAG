version: "3.9"

services:
  backend:
    build: ./backend
    container_name: document-qa-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: document-qa-frontend
    ports:
      - "4200:4200"
    depends_on:
      - backend